## skills-presets close-out check ({{REF}}, tracker: {{TRACKER}})

`mattpocock-skills`' `implement` has no completion step (its own docs say so), so it does not
do any of the following. Confirm each:

- [ ] The code has been merged back to the default branch with `--no-ff`
- [ ] Ticket {{REF}} has been closed using the **Resolve** convention in
      `docs/agents/issue-tracker.md`
- [ ] The worktree has been kept (not removed)

If any is outstanding, present the checklist and wait for the user. If the user already said
they do not want to merge, simply acknowledge.
