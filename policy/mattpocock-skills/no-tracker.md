## skills-presets: this repo has no issue tracker configured

`docs/agents/issue-tracker.md` does not exist, so assigning, closing, and worktree naming all
have nothing to work from.

Ask the user to run `/setup-matt-pocock-skills` first — that is exactly what the `code-review`
skill does in the same situation. **Do not guess between `gh`, `glab`, and local files**: a
wrong guess writes to the wrong place.
